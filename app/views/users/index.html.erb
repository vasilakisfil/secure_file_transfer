<% provide(:title, 'All users') %>

<div class="row">
  <div class="small-6 small-centered columns">
    <h1>All users</h1>

    <ul class="users">
      <% @users.each do |user| %>
        <li>
          <%= link_to user.username, user %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
